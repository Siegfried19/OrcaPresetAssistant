# 变更日志

按日期记录 Orca Preset Assistant 工作文件夹中的产品修改、部署状态和验证结果。

## 2026-08-11

### v0.6.3：提案闭环、原生 Undo 同步与可验证参数能力

- 首次连接改为选择普通父文件夹；软件自动创建标准目录和 `UserPresets/AGENTS.md`，但绝不覆盖已有操作规则。
- Orca 原生 `writeCapabilities` 增加参数名称、分类、编辑模式、面板可见性和回读验证元数据，并贯通 helper、面板与 Codex 插件。
- Codex 插件移除重复的 47/33 项静态白名单，直接使用 Orca 原生能力作为唯一写入依据。
- helper 原生状态发布接口接收 `writeCapabilities`；设置读取不再重复返回整份能力目录，降低原生桥接响应体积。
- 提案面板会标记当前 Orca 没有可见控件的隐藏参数；只有原生回执和新 revision 中的数值都匹配时，才显示“已应用并验证”。
- 移除 Prepare 参数区域额外插入的“当前预设已更新 / 查看修改 / 撤销”横条；提案面板保留验证和安全回滚，Orca 参数页只保留自身的复原与 Undo 交互。
- 首页固定为单张“最新修改”卡片：应用、拒绝、失败或回滚只更新同一张卡片，新建议到达时直接替换；回滚状态按当前生效方向显示参数变化。
- 统一切片页原生 Undo 与提案卡片状态：完全撤销自动标记为“已回滚”，部分撤销显示“部分回滚”，改成第三个值显示“已在 Orca 中调整”，Redo 可恢复“已应用”；卡片保存每个参数的当前值。
- revision conflict 会先同步 Orca 当前状态，不再直接向用户显示 `Native state revision is stale`；原生撤销、部分撤销和助手回滚现在共用同一份权威状态。
- 修复 Windows CI 临时 Git 仓库可能因路径别名被误判为不可用的问题；Git 调用失败时保留原始诊断作为错误原因。

验证状态：81 个应用测试通过、1 个按环境跳过，3 个 Codex 插件集成测试通过；实机完成提案写入、原生 Undo 自动回滚识别和新提案覆盖验证。完整结果见 `docs/VALIDATION.md`。

## 2026-08-10

### v0.6.2：可靠的参数提案写入与回执

- 将普通工艺参数白名单扩展到 47 项、普通耗材参数扩展到 33 项，并保持 machine 参数只读。
- 增加 `support_interface_speed` 与 `support_interface_loop_pattern`，用于 ABS 支撑界面速度和环形图案调整。
- 修复按喷嘴保存的多值参数写入：Codex 可以发送一个标量，Orca 原生端按当前真实槽位数量广播；显式多值仍必须保持现有数量。
- 统一面板对 Orca 数值、百分比、布尔值和广播向量的比较，避免 `20` 与权威回执 `20,20` 被误判为不同。
- 增加批准后回执恢复：即使即时回执持久化被中断，也会从稍后的 Orca 权威状态恢复成功或失败终态，不再留下无法批准也无法拒绝的孤儿提案。
- 原生能力声明增加 `scalarBehavior: broadcast-to-current-value-count`，数量错误会显示期望值数量和实际收到数量。

验证状态：应用、插件、补丁重放、Orca 原生测试与 Windows Release 构建结果见 `docs/VALIDATION.md`。

## 2026-08-06

### 全部用户预设父级兼容性复核（未改产品代码）

- 同时索引 Bambu Studio 官方 BBL 资源、FullVersion 当前 Orca BBL 资源和 UserPresets 中的全部 15 份用户 preset。
- 15 份 preset 共使用 10 个父级；当前全部能在 Orca 中按名称找到，`.info base_id` 与 Orca `setting_id` 一致。
- 除已迁移的 X1C PAHT-CF 外，其余 14 份父级在 Bambu 与 Orca 中名称和 ID 原本就一致；Orca 加载时已经解析到 Orca 自己的官方内容，因此没有批量改写同值字段。
- 15 份 preset 的 `from` 均为 `User`，不存在仍绑定 Bambu 运行时的来源标记。

验证状态：全量父级身份、直接父级、完整继承链和配套 `.info` 通过；现有面板继续显示 15 份 preset。本项为兼容性审计与日志补全，没有修改 helper 源码、构建产物或部署目录。

### 内嵌预设面板：保持列表可见并显示参数修改摘要

- 移除待处理提案启动时自动打开右侧 Inspector 的行为，避免参数详情遮住用户 preset 列表。
- 在用户预设列表上方增加修改摘要：目标 preset、状态、参数修改前/修改后、原因和“查看修改与写入位置”入口。
- 摘要同时覆盖待审批、等待 Orca 回执以及已由 Orca 应用且仍可安全回滚的修改。
- 首次 snapshot 读取失败时不再永久停留在“正在读取”，改为显示真实错误和重新读取按钮。
- Orca 真实参数页的“当前预设已更新 / 查看修改 / 撤销”回执继续由原生补丁 `0012` 提供，本次未移除或替换。

验证状态：TypeScript、ESLint、Prettier 和单元测试通过（64 项通过、1 项跳过）；生产构建和 Windows 目录包生成成功；已部署到 `FullVersion/OrcaSlicer/resources/helper`，部署前版本备份为 `helper.pre-visible-summary-20260806-004845.bak`，源/目标均为 74 个文件且主程序 SHA-256 一致。临时 helper 会话实测连接 15 个预设（9 个工艺、6 个材料），启动时 preset 列表可见且 Inspector 未自动打开；列表上方正确显示 `skirt_loops: 0 → 1` 的待审批摘要，点击入口后能打开写入位置详情；无会话令牌时会显示真实错误和“重新读取”，浏览器控制台无警告或错误。
